// ─── BACKWARD-COMPAT BRIDGE ───────────────────────────────────────────────────
// Canonical location: @core/database
// This file is kept so existing imports of "@/lib/prisma" continue to work.
export { prisma, default } from "@core/database";
